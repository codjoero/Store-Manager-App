language: python
python:
  - "3.6"

env:
  global:
    - APP_SETTINGS=testing

install:
  - "pip install -r requirements.txt"

script:
  - python3 -m pytest -v --cov APIs/tests --cov-report term-missing

after_success:
  - coveralls
  - codecov